(function(define) {

    'use strict';

    define(function() {

        return function() {
            var dept = win.PStax ? [win.PStax] : [];
            return dept;
        };

    });

})(window.define);
