/**
 * Extends Universal, page level keyvalues with wp mobile specific keyvalues
 */
(function(w, d, define){

  'use strict';

  define(['utils.core', 'pageKeyvalues.core'], function(utils, pageKeyvalues){

    return utils.extend(pageKeyvalues, {

    });

  });

})(window, document, window.define);